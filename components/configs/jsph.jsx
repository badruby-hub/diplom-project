export const
config = {
    columns: [
        { title: 'id', content: ({ id }) => id },
        { title: 'name', content: ({name}) => name },
        { title: 'username', content: ({ username }) => username },
        
    ]
};